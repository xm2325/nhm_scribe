extends: configs/real_eval_10_rule.yaml
experiment:
  mode: real_image
  title: Herbarium SCRIBE Real-Image Eval 100 DeepSeek V4 Pro Report
sampling:
  demo_size: 4
  eval_size: 100
  stratify_by: institutionCode
  prefer_institution_disjoint: true
image:
  output_subdir: real_eval_100
outputs:
  prefix: real_eval_100
  prediction_name: rule
